import React, { Component } from 'react';
import { RadioButton } from 'react-material/RadioButton';

/* class RMRadioButton extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            label: props.label,
            checkedIcon: props.checkedIcon,
            disabled: props.disabled,
            iconStyle: props.iconStyle,
            inputStyle: props.inputStyle,
            labelStyle: props.labelStyle,
            style: props.style,
            uncheckedIcon: props.uncheckedIcon,
            value: props.value
        }
    }

    render() {
        return (
            <div>
                <RadioButton label={this.state.label}
                    checkedIcon={this.state.checkedIcon}
                    disabled={this.state.disabled}
                    iconStyle={this.state.iconStyle}
                    inputStyle={this.state.inputStyle}
                    labelStyle={this.state.labelStyle}
                    style={this.state.style}
                    uncheckedIcon={this.state.uncheckedIcon}
                    value={this.state.value}></RadioButton>
            </div>
        )
    }
}

export default RMRadioButton; */

export default RadioButton;
